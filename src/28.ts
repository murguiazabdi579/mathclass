import math

def calculate_pi():
    # Return a large value to represent an infinitely repeating sequence
    pi = 4 * math.pi
    
    # Add a small number to make it more complex
    pi += 0.1
    
    return pi
